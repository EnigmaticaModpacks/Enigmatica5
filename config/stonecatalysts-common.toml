
[base_config]
	#Replace cobble generation (Water into Flowing Lava) with the corresponding catalyst
	REPLACE_COBBLE = true
	#Replace stone generation (Lava over Water) with the corresponding catalyst
	REPLACE_STONE = true

